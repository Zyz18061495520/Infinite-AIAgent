# Agent Admin

[English](./README.md) | [中文 (简体)](#中文)

---

### 📖 简介

Agent Admin 是一个现代化的、功能丰富的管理后台系统，用于管理 AI 智能体。基于 Vue 3、Vite 5、TypeScript 和 Naive UI 构建，提供了创建、管理和部署智能体的综合平台，支持多种智能体类型、模型、工具和集成。

### ✨ 功能特性

- 🤖 **智能体管理**: 创建和管理智能体，支持单智能体、工作流和监督者模式
- 🧠 **模型管理**: 配置和管理来自多个提供商的 AI 模型（DeepSeek、OpenAI、Qwen、Ollama）
- 🛠️ **工具管理**: 集成和管理各种工具以增强智能体能力
- 💾 **数据库管理**: 连接和管理数据库资源
- 📚 **RAG（检索增强生成）**: 管理知识库和 RAG 配置
- ❓ **FAQ 管理**: 创建和管理常见问题
- 🔌 **MCP（模型上下文协议）**: 管理 MCP 集成
- 👥 **工作空间管理**: 多工作空间支持，包含成员管理
- 🏪 **应用商店**: 浏览和部署预构建的智能体应用
- 🌐 **国际化**: 内置支持英文和中文
- 🎨 **现代化 UI**: 基于 Naive UI 构建的简洁直观界面
- 🌓 **暗色模式**: 支持亮色和暗色主题
- 📱 **响应式设计**: 在桌面和移动设备上无缝工作

### 🛠️ 技术栈

- **框架**: Vue 3 (Composition API)
- **构建工具**: Vite 5
- **语言**: TypeScript
- **UI 库**: Naive UI
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP 客户端**: Alova
- **样式**: UnoCSS, SCSS
- **图标**: Iconify
- **编辑器**: Monaco Editor、Markdown 编辑器、富文本编辑器
- **图表**: ECharts
- **工作流**: Vue Flow
- **国际化**: Vue I18n

### 📦 环境要求

- Node.js >= 18.x
- pnpm >= 8.x（推荐）或 npm/yarn

### 🚀 快速开始

#### 安装

```bash
# 克隆仓库
git clone https://github.com/your-org/agent-admin.git

# 进入项目目录
cd agent-admin

# 安装依赖
pnpm install
```

#### 开发

```bash
# 启动开发服务器
pnpm dev

# 应用将在 http://localhost:9980 可用
```

#### 构建

```bash
# 构建生产版本
pnpm build

# 构建开发环境版本
pnpm build:dev

# 预览生产构建
pnpm preview
```

#### 代码检查

```bash
# 检查代码规范
pnpm lint

# 自动修复代码规范问题
pnpm lint:fix

# 打开 ESLint 配置检查器
pnpm lint:check
```

### 📁 项目结构

```
agent-admin/
├── build/              # 构建配置文件
├── dist/               # 生产构建输出
├── docker/             # Docker 配置文件
├── locales/            # 国际化文件
│   ├── en_US.json     # 英文翻译
│   └── zh_CN.json     # 中文翻译
├── public/             # 静态资源
├── src/
│   ├── assets/        # 图片、图标和其他资源
│   ├── components/    # 可复用的 Vue 组件
│   ├── constants/     # 应用常量
│   ├── directives/    # Vue 指令
│   ├── hooks/         # Composition API 钩子
│   ├── layouts/       # 布局组件
│   ├── modules/       # 模块配置
│   ├── router/        # Vue Router 配置
│   ├── service/       # API 服务和 HTTP 客户端
│   ├── store/         # Pinia 状态管理
│   ├── styles/        # 全局样式
│   ├── typings/       # TypeScript 类型定义
│   ├── utils/         # 工具函数
│   └── views/         # 页面组件
├── Dockerfile          # Docker 配置
├── Jenkinsfile         # CI/CD 流水线配置
├── package.json        # 项目依赖和脚本
├── tsconfig.json       # TypeScript 配置
├── unocss.config.ts    # UnoCSS 配置
└── vite.config.ts      # Vite 配置
```

### 🐳 Docker 部署

#### 构建 Docker 镜像

```bash
docker build -t agent-admin:latest .
```

#### 运行 Docker 容器

```bash
docker run -d \
  --name agent-admin \
  -p 80:80 \
  agent-admin:latest
```

### 🔧 配置

#### 环境变量

在项目根目录创建 `.env` 文件：

```env
# 应用基础路径
VITE_BASE_URL=/

# 路由模式: 'hash' 或 'history'
VITE_ROUTE_MODE=hash

# API 基础地址
VITE_API_BASE_URL=http://localhost:8080/api
```

#### 服务配置

编辑 `service.config.ts` 以配置 API 端点和其他服务设置。

### 📝 可用脚本

- `pnpm dev` - 启动开发服务器
- `pnpm dev:prod` - 以生产模式启动开发服务器
- `pnpm build` - 构建生产版本
- `pnpm build:dev` - 构建开发环境版本
- `pnpm preview` - 预览生产构建
- `pnpm lint` - 运行 ESLint 和 TypeScript 类型检查
- `pnpm lint:fix` - 自动修复代码规范问题
- `pnpm lint:check` - 打开 ESLint 配置检查器
- `pnpm sizecheck` - 分析打包体积

### 🤝 贡献

欢迎贡献！请随时提交 Pull Request。
